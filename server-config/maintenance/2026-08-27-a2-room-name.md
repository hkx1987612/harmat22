# A2-3-L1 Room Name Correction

## Scope

- Public URL: `https://harmat22.hu/property/a2-3-l1/`
- WordPress property: `5205`, published, title `A2-3-L1`.
- Target: `A2-3-L1/08`, `18.34 m2`, `Nappali` -> `Szoba`.
- Preserve the actual living room `A2-3-L1/04`, `Nappali`, `22.68 m2`.
- Sources: one HTML string in Elementor widget `4520a1d` and one matching text block in `post_content`.
- No changes to room count (`4`), interior area (`96.04`), sales area (`111.57`), price (`139655250` HUF), status, PDF, image, template, CSS, JavaScript, CRM, Ads or authentication settings.

## Execution

- Pulled GitHub main from a clean working tree before work.
- Repair: `2026-08-27-correct-a2-room-name.php`; focused tests: `2026-08-27-test-a2-room-name.php`.
- Both scripts were uploaded to temporary files in the private backup directory, hash-checked, linted, renamed to their final filenames and linted again. They are not installed as public plugins or recurring hooks.
- All 11 tests passed both locally and on the server. The live dry run checked exact source hashes and all target identifiers before writing.
- A verified, exclusive-create JSON backup was written before the first database change. It contains the original property fields and metadata, original hashes, and expected corrected values. Directory mode is `0700`; backup file mode is `0600`.
- Elementor metadata was updated with an expected-old-value guard. The fallback body used a WordPress database compare-and-swap update to preserve all other post fields and avoid unrelated save hooks.
- Post and metadata verification ran before clearing Elementor/page/object caches. Automatic rollback restores only values still matching this operation if the write or verification fails.
- A later dry run reported `Already corrected`; the script does not repeat the write.

Backup directory:

`/home/harmath2/codex-backups/a2-room-name-20260827-154745`

Backup file:

`/home/harmath2/codex-backups/a2-room-name-20260827-154745/property-5205-before.json`

## Verification

- Deep JSON comparison preserving object/array types found only `root.1.elements.1.elements.1.elements.1.settings.html` changed. The HTML value became two bytes shorter, matching the room-name correction.
- All 124 property-body hashes were compared; only `5205` changed.
- All 34 existing offer leads and the server error-log size/timestamp remained unchanged. No live test inquiry or conversion was created.
- Existing public regression scanner: 145 pages, 124 properties, 573 resources; zero sitemap, page, property or resource issues.
- Chrome desktop: `/08` is `Szoba 18,34 m2`; `/04` is `Nappali 22,68 m2`; the floor-plan image loads; no horizontal overflow or console errors were observed.
- Offer modal opens with A2-3-L1 selected, sales area 111.57 m2, price 139,655,250 Ft and all five source choices. The modal was closed without submission.
- Browser verification was a desktop visual/interaction check plus the site-wide HTTP/content scan, not a new full cross-device functional audit. No layout styles changed.
- Generated scan details remain local in `outputs/a2-room-name-scan-20260827.json`, excluded from Git.

## Rollback

Git contains the reproducible repair and tests, not the site's database or private backup. Checking out a previous tag does not restore this content change.

For an intentional rollback, use the private JSON snapshot and first confirm property `5205` is still `A2-3-L1`. Compare current `post_content` and `_elementor_data` to the snapshot's `expected_content` and `expected_elementor`. Stop on drift rather than overwriting a later editor's changes. Restore only `post.post_content` and `meta._elementor_data[0]`, using expected-current-value guards as in the repair script's rollback branch. Preserve unrelated fields and later data. Clear caches and recheck the property, the living-room row, offer modal and key public pages. Do not restore the whole database for this single label.
